server:
  servlet:
    context-path: /api/v1
spring:
  application:
    name: MarketPlace
  datasource:
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    default-schema: public
